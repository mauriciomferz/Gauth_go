# Simple Dockerfile for mock server (Linux AMD64)
FROM golang:1.23-alpine AS builder
WORKDIR /build
COPY cmd/mock-server/main.go .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o server main.go

FROM alpine:3.19
WORKDIR /app
COPY --from=builder /build/server .
EXPOSE 8080
ENV VERSION=blue
CMD ["/app/server"]
