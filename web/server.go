package web

// Placeholder file.
// The actual server implementation lives in `server_clean.go` (same package)
// and the runnable entrypoint (main function) is in `cmd/web-server/main.go`.
// This file is retained to avoid build confusion and can host package-level
// documentation in the future.
