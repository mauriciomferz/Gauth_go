# Kubernetes Deployment for AgentAuth with JWE Encryption
# Includes: Deployment, Service, ConfigMap, Secrets, HPA

---
# Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: agentauth
  labels:
    app: agentauth
    environment: production

---
# ConfigMap for JWE Configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: agentauth-jwe-config
  namespace: agentauth
data:
  AGENTAUTH_JWE_ENABLED: "true"
  AGENTAUTH_JWE_ALGORITHM: "RSA-OAEP-256"
  AGENTAUTH_JWE_ENCRYPTION: "A256GCM"
  AGENTAUTH_JWE_KEY_ID: "agentauth-prod-2025-11"
  AGENTAUTH_JWE_ROTATION_DAYS: "365"
  AGENTAUTH_PORT: "8080"
  AGENTAUTH_LOG_LEVEL: "info"

---
# Secret for JWE Keys (base64 encoded PEM files)
apiVersion: v1
kind: Secret
metadata:
  name: agentauth-jwe-keys
  namespace: agentauth
type: Opaque
data:
  # Base64 encoded RSA public key PEM
  # Generate: cat public.pem | base64 -w 0
  public.pem: LS0tLS1CRUdJTi... # Replace with actual base64 encoded public key
  
  # Base64 encoded RSA private key PEM
  # Generate: cat private.pem | base64 -w 0
  private.pem: LS0tLS1CRUdJTi... # Replace with actual base64 encoded private key

---
# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentauth-server
  namespace: agentauth
  labels:
    app: agentauth
    component: authorization-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: agentauth
      component: authorization-server
  template:
    metadata:
      labels:
        app: agentauth
        component: authorization-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      # Security context
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      
      # Init container to validate keys
      initContainers:
      - name: validate-keys
        image: alpine:latest
        command: ['sh', '-c', 'test -f /etc/agentauth/keys/public.pem && test -f /etc/agentauth/keys/private.pem']
        volumeMounts:
        - name: jwe-keys
          mountPath: /etc/agentauth/keys
          readOnly: true
      
      containers:
      - name: agentauth
        image: agentauth:jwe-latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        
        # Environment from ConfigMap
        envFrom:
        - configMapRef:
            name: agentauth-jwe-config
        
        # Environment from Secrets
        env:
        - name: AGENTAUTH_JWE_PUBLIC_KEY
          value: "/etc/agentauth/keys/public.pem"
        - name: AGENTAUTH_JWE_PRIVATE_KEY
          value: "/etc/agentauth/keys/private.pem"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: agentauth-db-credentials
              key: connection-string
        
        # Volume mounts
        volumeMounts:
        - name: jwe-keys
          mountPath: /etc/agentauth/keys
          readOnly: true
        
        # Resource limits
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 512Mi
        
        # Health checks
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        
        # Security context
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      
      # Volumes
      volumes:
      - name: jwe-keys
        secret:
          secretName: agentauth-jwe-keys
          defaultMode: 0400
      
      # Node affinity (optional - spread across zones)
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - agentauth
              topologyKey: kubernetes.io/hostname

---
# Service
apiVersion: v1
kind: Service
metadata:
  name: agentauth-service
  namespace: agentauth
  labels:
    app: agentauth
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: agentauth
    component: authorization-server

---
# Horizontal Pod Autoscaler
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agentauth-hpa
  namespace: agentauth
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agentauth-server
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

---
# Ingress (example - adjust for your ingress controller)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agentauth-ingress
  namespace: agentauth
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - auth.example.com
    secretName: agentauth-tls
  rules:
  - host: auth.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: agentauth-service
            port:
              number: 8080
