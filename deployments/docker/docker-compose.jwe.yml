
services:
  # AgentAuth Authorization Server with JWE Encryption
  agentauth-server:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile.jwe
    image: agentauth:jwe-latest
    container_name: agentauth-server
    ports:
      - "8080:8080"
    environment:
      # JWE Configuration
      AGENTAUTH_JWE_ENABLED: "true"
      AGENTAUTH_JWE_ALGORITHM: "RSA-OAEP-256"
      AGENTAUTH_JWE_ENCRYPTION: "A256GCM"
      AGENTAUTH_JWE_PUBLIC_KEY: "/etc/agentauth/keys/public.pem"
      AGENTAUTH_JWE_PRIVATE_KEY: "/etc/agentauth/keys/private.pem"
      AGENTAUTH_JWE_KEY_ID: "agentauth-prod-2025-11"
      AGENTAUTH_JWE_ROTATION_DAYS: "365"
      
      # Application Configuration
      AGENTAUTH_PORT: "8080"
      AGENTAUTH_LOG_LEVEL: "info"
      
      # Database (example)
      DATABASE_URL: "postgresql://agentauth:secret@postgres:5432/agentauth?sslmode=disable"
    
    volumes:
      # Mount JWE keys from host (SECURE: use secrets in production)
      - ./keys:/etc/agentauth/keys:ro
      
      # Alternative: Mount keys from Docker volume
      # - agentauth-keys:/etc/agentauth/keys:ro
    
    secrets:
      # Production: Use Docker secrets for sensitive keys
      - source: agentauth_private_key
        target: /run/secrets/private.pem
        mode: 0400
    
    networks:
      - agentauth-network
    
    depends_on:
      - postgres
      - redis
    
    restart: unless-stopped
    
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: agentauth-postgres
    environment:
      POSTGRES_USER: agentauth
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: agentauth
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - agentauth-network
    restart: unless-stopped

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: agentauth-redis
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    networks:
      - agentauth-network
    restart: unless-stopped

  # Prometheus (Monitoring)
  prometheus:
    image: prom/prometheus:latest
    container_name: agentauth-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"
    networks:
      - agentauth-network
    restart: unless-stopped

  # Grafana (Visualization)
  grafana:
    image: grafana/grafana:latest
    container_name: agentauth-grafana
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana-dashboards:/etc/grafana/provisioning/dashboards:ro
    ports:
      - "3000:3000"
    networks:
      - agentauth-network
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  postgres-data:
  redis-data:
  prometheus-data:
  grafana-data:
  # Uncomment if using Docker volumes for keys
  # agentauth-keys:

networks:
  agentauth-network:
    driver: bridge

secrets:
  agentauth_private_key:
    file: ./keys/private.pem
