apiVersion: 1

providers:
  - name: 'GAuth+ Dashboards'
    orgId: 1
    folder: 'GAuth+'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
