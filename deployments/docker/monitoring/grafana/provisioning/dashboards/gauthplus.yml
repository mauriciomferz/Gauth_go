apiVersion: 1

providers:
  - name: 'AgentAuth+ Dashboards'
    orgId: 1
    folder: 'AgentAuth+'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
