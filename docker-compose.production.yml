###############################################
# AgentAuth Production Docker Compose
# Full-stack deployment with backend + frontend
###############################################

services:
  # Backend Service
  backend:
    build:
      context: .
      dockerfile: Dockerfile.production
      target: runtime
    container_name: gauth-backend-prod
    restart: unless-stopped
    ports:
      - "8090:8080"
    environment:
      - GAUTH_ENV=production
      - GAUTH_PORT=8080
      - GAUTH_HOST=0.0.0.0
      - GAUTH_CORS_ALLOW=${GAUTH_CORS_ALLOW:-https://gauth.example.com}
      - GAUTH_LOG_LEVEL=${GAUTH_LOG_LEVEL:-warn}
      - GAUTH_LOG_FORMAT=json
      - GAUTH_METRICS_ENABLED=true
      - GAUTH_ENABLE_PROMETHEUS=true
      - GAUTH_JWT_SIGNING_KEY=${GAUTH_JWT_SIGNING_KEY}
      - GAUTH_RFC0111_ENABLED=1
      - GAUTH_REVOCATION_ENABLED=1
      - GAUTH_ANCHOR_PERSIST_PATH=/app/data/anchor.json
      - GAUTH_EDDSA_PERSIST_PATH=/app/data/keys.json
      - GAUTH_ISSUER=http://localhost:8090
      - GAUTH_DB_HOST=${GAUTH_DB_HOST:-postgres}
      - GAUTH_DB_PORT=5432
      - GAUTH_DB_USER=${GAUTH_DB_USER}
      - GAUTH_DB_PASSWORD=${GAUTH_DB_PASSWORD}
      - GAUTH_DB_NAME=${GAUTH_DB_NAME:-gauth_production}
      - GAUTH_DB_SSL_MODE=disable
      - DB_HOST=${GAUTH_DB_HOST:-postgres}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - gauth-data:/app/data
      - gauth-logs:/app/logs
    networks:
      - gauth-network
    healthcheck:
      test: [ "CMD", "/app/web-server", "-healthcheck" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  # Frontend Service (Nginx serving static build)
  frontend:
    build:
      context: ./frontend/ui-react
      dockerfile: Dockerfile.production
    container_name: gauth-frontend-prod
    restart: unless-stopped
    ports:
      - "3002:8080"
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL:-/api/v1}
      - VITE_ENV=production
    networks:
      - gauth-network
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      - backend

  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: gauth-postgres-prod
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${GAUTH_DB_USER}
      - POSTGRES_PASSWORD=${GAUTH_DB_PASSWORD}
      - POSTGRES_DB=${GAUTH_DB_NAME:-gauth_production}
      - POSTGRES_INITDB_ARGS=--encoding=UTF-8 --lc-collate=en_US.UTF-8 --lc-ctype=en_US.UTF-8
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./schema/migrations:/docker-entrypoint-initdb.d:ro
    networks:
      - gauth-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${GAUTH_DB_USER} -d ${GAUTH_DB_NAME:-gauth_production}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: gauth-redis-prod
    restart: unless-stopped
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    networks:
      - gauth-network
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Prometheus Monitoring
  prometheus:
    image: prom/prometheus:latest
    container_name: gauth-prometheus-prod
    restart: unless-stopped
    ports:
      - "9091:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=30d'
    volumes:
      - ./monitoring/prometheus-prod.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - gauth-network
    depends_on:
      - backend

  # Grafana Visualization
  grafana:
    image: grafana/grafana:latest
    container_name: gauth-grafana-prod
    restart: unless-stopped
    ports:
      - "3005:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=https://grafana.gauth.example.com
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana-dashboards:/etc/grafana/provisioning/dashboards:ro
    networks:
      - gauth-network
    depends_on:
      - prometheus

volumes:
  gauth-data:
    driver: local
  gauth-logs:
    driver: local
  postgres-data:
    driver: local
  redis-data:
    driver: local
  prometheus-data:
    driver: local
  grafana-data:
    driver: local

networks:
  gauth-network:
    driver: bridge
