apiVersion: apps/v1
kind: Deployment
metadata:
  name: gauth-green
  namespace: gauth-staging
  labels:
    app: gauth
    version: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gauth
      version: green
  template:
    metadata:
      labels:
        app: gauth
        version: green
    spec:
      containers:
      - name: gauth
        image: gauth:green-v2
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: GAUTH_WEB_PORT
          value: "8080"
        - name: GAUTH_ENV
          value: "staging"
        - name: GAUTH_LOG_LEVEL
          value: "info"
        - name: VERSION
          value: "green"
        resources:
          requests:
            memory: 256Mi
            cpu: 200m
          limits:
            memory: 512Mi
            cpu: 500m
        livenessProbe:
          httpGet:
            path: /api/v1/beta/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/beta/health
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 5
