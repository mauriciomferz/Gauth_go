apiVersion: apps/v1
kind: Deployment
metadata:
  name: gauth-green
  namespace: gauth-staging
  labels:
    app: gauth
    version: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gauth
      version: green
  template:
    metadata:
      labels:
        app: gauth
        version: green
    spec:
      containers:
      - name: gauth
        image: ghcr.io/mauriciomferz/gauth:green
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: GAUTH_WEB_PORT
          value: "8080"
        - name: GAUTH_ENV
          value: "staging"
        - name: GAUTH_LOG_LEVEL
          value: "info"
        - name: VERSION
          value: "green"
        - name: VERSION
          value: "green"
        - name: GAUTH_REVOCATION_ENABLED
          value: "1"
        - name: GAUTH_JWT_SIGNING_KEY
          value: "staging-secret-key-change-in-prod"
        resources:
          requests:
            memory: 256Mi
            cpu: 200m
          limits:
            memory: 512Mi
            cpu: 500m
        livenessProbe:
          httpGet:
            path: /api/v1/beta/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/beta/health
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 5
