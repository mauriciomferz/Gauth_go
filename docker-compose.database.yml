
services:
  postgres:
    image: postgres:15-alpine
    container_name: agentauth-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: agentauth_dev_password
      POSTGRES_DB: agentauth
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d agentauth"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - agentauth-network

  # Optional: PostgreSQL Admin UI (pgAdmin)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: agentauth-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@agentauth.local
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    restart: unless-stopped
    networks:
      - agentauth-network
    profiles:
      - with-ui  # Only start with: docker-compose --profile with-ui up

volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local

networks:
  agentauth-network:
    driver: bridge
